{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile --prod=false && pnpm turbo run build --filter=@budi/api...",
  "buildCommand": "pnpm --filter @budi/web run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
