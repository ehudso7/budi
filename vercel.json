{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile --prod=false && pnpm --filter @budi/contracts run build && cd services/api && (pnpm prisma db push --skip-generate || echo 'Prisma db push skipped - DATABASE_URL may not be set') && cd ../.. && pnpm --filter @budi/api run build",
  "buildCommand": "pnpm --filter @budi/web run build",
  "outputDirectory": "../../apps/web/.next",
  "framework": "nextjs"
}
