{
  "name": "@masterforge/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=../../.env ./src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint later in Step 2')\"",
    "test": "node -e \"console.log('tests later in Step 2')\""
  },
  "dependencies": {
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}