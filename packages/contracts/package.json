{
  "name": "@budi/contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('No lint configured for contracts')\"",
    "test": "node -e \"console.log('No tests for contracts')\""
    "lint": "node -e \"console.log('No lint configured for contracts')\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}